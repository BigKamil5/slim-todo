<nav class="navbar navbar-expand-lg navbar-dark bg-dark pr-5 pl-5">
    <a class="navbar-brand" href="#">ToDoApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
  
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          <a class="nav-link" href="{{ base_url() }}">Home <span class="sr-only">(current)</span></a>
        </li>
      </ul>
      <ul class="navbar-nav ml-auto">
      {% if user.check %}
        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"> {{user.user.name}} </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <a class="dropdown-item" href="{{ path_for('auth.signout') }}" >Sign Out</a>
            <a class="dropdown-item" href="#">Change Password</a>
            </div>
        </li>
      {% else %}
  
        <li class="nav-item active">
          <a href="{{ path_for('auth.signin') }}" class="nav-link">Sign In</a>
        </li>
        <li class="nav-item active">
          <a href="{{ path_for('auth.index') }}" class="nav-link">Sign Up</a>
        </li>
        {% endif %}        
      
      </ul>
      
    </div>
  </nav>